{
  "name": "@idfon/mac",
  "description": "AppKit version of Idfon (macOS).",
  "private": true,
  "scripts": {
    "clean": "rm -rf build .build",
    "build": "bash build.sh",
    "run": "open build/Idfon.app"
  }
}
